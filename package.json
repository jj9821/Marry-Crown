{
  "name": "mary-crown-restaurant",
  "version": "1.0.0",
  "private": true,
  "description": "Mary Crown Restaurant Digital Menu",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html assets manifest.json robots.txt sitemap.xml dist/ && cp -r public/* dist/ 2>/dev/null || true"
  }
}