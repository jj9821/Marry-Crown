{
  "name": "mary-crown-restaurant",
  "version": "1.0.0",
  "private": true,
  "description": "Mary Crown Restaurant Digital Menu & Owner Portal",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html admin.html admin assets manifest.json robots.txt sitemap.xml dist/ 2>/dev/null || true && cp -r public/* dist/ 2>/dev/null || true"
  }
}